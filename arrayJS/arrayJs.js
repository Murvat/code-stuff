Добавление/удалениеэлементов
// let array =[];
// array.pop();
// array.push();
// array.shift();
// array.unshift();
// array.splice( start[, deleteCount, elem1...elemN ])
// array.slice([star], [end])
// array.concat(arg1, arg2...)
Перебор:forEach
// array.forEach((item, index, array){ function });
Поисквмассиве
// array.indexOf(item, from)
// array.includes(item, from)
// array.lastIndexOf(item, from)
// array.find(function(item, index, array){function});
// array.findIndex()
// array.findLastIndex()
// array.filter(function(item,index, array){...})
Преобразованиемассива
// array.map(function(item, index, array){...})
// array.sort(function)
// array.reverse();
// array = str.split(delim, elemCount)
// str = array.joim(glue)
// arrayy.reduce(
// function(accumulator, item, index, array){...}, [initial])
// array.some(fn)
// array.every(fn)
// arr.fill(value, start, end)
// arr.copyWithin(target, start, end)
// arr.flat(depth)
// arr.flatMap(fn)